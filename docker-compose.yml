name: acestep-local-dev

networks:
  local-net:
    driver: bridge

volumes:
  local_rabbitmq_data_acestep:
  local_minio_data_acestep:
  local_audio_acestep_model_cache:

services:
  # 1. BAĞIMLILIK: RABBITMQ
  rabbitmq:
    image: ghcr.io/sentiric/sentiric-rabbitmq:latest
    container_name: local-rabbitmq-acestep
    environment:
      - RABBITMQ_DEFAULT_USER=sentiric
      - RABBITMQ_DEFAULT_PASS=sentiric_pass
    ports:
      - "5672:5672"
      - "15672:15672"      
    networks:
      - local-net
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. BAĞIMLILIK: MINIO
  minio:
    image: ghcr.io/sentiric/sentiric-minio:latest
    container_name: local-minio-acestep
    environment:
      - MINIO_ROOT_USER=sentiric
      - MINIO_ROOT_PASSWORD=sentiric-secret-key
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"    
    networks:
      - local-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/ready"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio-init:
    image: minio/mc
    depends_on:
      minio:
        condition: service_healthy
    networks:
      - local-net
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 sentiric sentiric-secret-key;
      mc mb myminio/sentiric --ignore-existing;
      mc anonymous set download myminio/sentiric;
      echo '✅ Local MinIO Bucket (sentiric) is ready.';
      "

  # 3. ANA SERVİS: AUDIO-ACESTEP
  audio-acestep-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: local-acestep-service
    environment:
      - ENV=development
      - MUSIC_MODEL_ID=facebook/musicgen-small
      - GRPC_TLS_CA_PATH=/sentiric-certificates/certs/ca.crt
      - ACESTEP_SERVICE_CERT_PATH=/sentiric-certificates/certs/audio-acestep-service-chain.crt
      - ACESTEP_SERVICE_KEY_PATH=/sentiric-certificates/certs/audio-acestep-service.key
      - BUCKET_ENDPOINT_URL=http://minio:9000
      - BUCKET_ACCESS_KEY_ID=sentiric
      - BUCKET_SECRET_ACCESS_KEY=sentiric-secret-key
      - BUCKET_NAME=sentiric
      - RABBITMQ_URL=amqp://sentiric:sentiric_pass@rabbitmq:5672/%2f
    volumes:
      - "../sentiric-certificates:/sentiric-certificates:ro"
      - local_audio_acestep_model_cache:/app/model-cache
    ports:
      - "16310:16310" # HTTP
      - "16311:16311" # gRPC
    depends_on:
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - local-net
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]